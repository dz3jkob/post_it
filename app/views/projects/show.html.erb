<p id="notice"><%= notice %></p>

<p>
  <strong>Project Description:</strong>
  <%= @project.name %>
</p>

<p>
  <strong>User:</strong>
  <%= @project.user.email %>
</p>

<p>
  <strong>Description:</strong>
  <%= @project.description %>
</p>

<% if @project.user == current_user %>
	<%= link_to 'Edit', edit_project_path(@project) %> |
<% end %>
<%= link_to 'Back', projects_path %>

<p>
  <strong>Cards:</strong>
</p>

<% @cards = Card.all %>
<%= render 'index_cards.html.erb' %>

